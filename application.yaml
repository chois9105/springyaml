string:
  test: 'application-string-tests'

spring:
  rabbitmq: # RabbitMQ 관련 설정
    host: localhost
    port: 5672
    username: madplay
    password: madplay

management:
  endpoints:
    web:
      exposure:
        include: busrefresh